apiVersion: v1
kind: Service
metadata:
  name: python-flaskapp
  namespace: flaskapp

spec:
  type: NodePort

  selector:
    app: python-flaskapp

  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30081
